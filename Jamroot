
constant VERSION : [ MATCH "\\$Revision: *([0-9]*) *\\$" : "$Revision: 4185 $" ] ;

project /xylose
    : requirements
        <include>src
    : usage-requirements
        <include>src
    ;

#lib boost_unit_test_framework : : <name>boost_unit_test_framework-mt <link>static ;
use-project /boost : ../boost ;

lib xylose-lib
    : # sources
      src/xylose/power.c
      src/xylose/logger.c
      [ glob src/xylose/*.cpp ]
    : <link>static
      <include>src
    ;

alias xylose
    : # sources
      xylose-lib
      [ glob src/xylose/*.h ] # olson-tools stuff is currently still .h
      [ glob src/xylose/*.hpp ]
      [ glob src/xylose/detail/*.hpp ]
      [ glob src/xylose/random/*.hpp ]
      [ glob src/xylose/random/detail/*.hpp ]
    : # no build requirements
    : # no default build
    : <include>src ;

